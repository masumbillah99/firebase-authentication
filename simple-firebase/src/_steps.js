/**
    ------------------
    INSTALL SETUP
    ------------------
 * visit: console.firebase.google.com
 * create project (skip google analytics)
 * Register app (create config)
 * install firebase
 * add config file to your project
 * DANGER: Do not publish or make firebase config to public by pushing those to github.
    -------------------
    INTEGRATION
    -------------------
* go to docs -> build -> authentication -> web
 * export app from the firebase.config.js file: export default app
 * Login.jsx: import getAuth from firebase/auth
 * create, const auth = getAuth(app)
    -----------------
    PROVIDER SETUP
    -----------------
 * import googleAuthProvide and create a new provider
 * use signInWithPopUp and (pass auth and provider)
 * activate sign-in method (google, facebook, github etc.)
 * change domain name (localhost:port-number)
 * [vite]: change 127.0.0.1 to localhost
 
    -----------------
    More AUTH PROVIDER
    -----------------
 1. activate the auth provider (create app, provide redirect url, client id, client secret)

 */


//  Email Password authentication, and Login form
/** 
    1. 
    2. 
    3. 
    4.
    5.
*/